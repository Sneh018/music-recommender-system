# 🎵 Music Recommender System

An interactive Music Recommender System built with Python, Streamlit, and NLP**.  
It suggests similar songs based on lyrical similarity using TF-IDF vectorization and cosine similarity.

# 🚀 Features
- Recommends songs based on lyrical content similarity.  
- Uses NLP preprocessing (tokenization, stemming, TF-IDF).  
- Integrated with Spotify API (Spotipy) to display real album covers.  
- Lightweight sample dataset (`small_spotify_sample.csv`) for fast demos.  

# 🧠 Tech Stack
Python, Streamlit, Spotipy, scikit-learn, nltk, pandas`

# 🖥️ How to Run
```bash
pip install -r requirements.txt
streamlit run app.py
